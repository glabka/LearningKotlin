public class Main {
    public static void main(String[] args) {
        // imported from kotlin
        Parameters params = new Parameters();
        // because of @JvmOveloads this overloaded method is created
        params.funWithDefaultParams(4);
    }
}
